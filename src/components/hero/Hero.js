import React from "react";
import './Hero.css';
import { Paper } from "@mui/material";

const Hero = ({flights}) => {
  return (
      <div>
      {/*this is where the filter/search bar should be*/}
      </div>
  )
}

export default Hero